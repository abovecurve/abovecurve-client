import React from "react";
import styles from "./landing.module.scss";

const Landing = (props) => {
  return (
    <div className={styles.Landing}>
      Landing Component
      <ul>Testing Sass installation</ul>
    </div>
  );
};

export default Landing;
